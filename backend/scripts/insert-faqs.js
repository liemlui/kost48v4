/**
 * USANG — insert-faqs.js sudah tidak digunakan.
 *
 * FAQ kanonik ada di: backend/src/modules/faqs/faqs.service.ts → DEFAULT_FAQS
 * Seed via endpoint (idempoten):
 *   POST http://localhost:3000/api/faqs/seed  (perlu login OWNER)
 *
 * Referensi data lengkap: docs/DEFAULT_DATA.md
 */
console.error('⛔ insert-faqs.js USANG. Seed FAQ via: POST /api/faqs/seed (lihat docs/DEFAULT_DATA.md)');
process.exit(1);
