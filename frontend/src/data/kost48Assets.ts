// Stub: no bundled room cover assets. UI falls back to initials placeholder.
export function getKost48RoomCover(
  _code?: string | null,
  _name?: string | null,
): string | null {
  return null;
}
